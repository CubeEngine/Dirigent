package de.cubeisland.engine.formatter;

public class MessageCompositor
{
    public String composeMessage(String source, Object... arguments)
    {
        StringBuilder finalMessage = new StringBuilder();
        // {[[<position>:]type[:<flags>]]}
        // TODO
        return finalMessage.toString();
    }
}
